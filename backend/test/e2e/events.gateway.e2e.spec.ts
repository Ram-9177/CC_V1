/**
 * Socket e2e scaffold (skipped by default).
 * To enable, run with ENABLE_SOCKET_E2E=true in env and wire a test app bootstrap.
 */

describe('EventsGateway e2e (optional)', () => {
  it('connects and joins rooms (placeholder)', async () => {
    // Intentionally minimal to avoid CI flakiness. Wire a real bootstrap if/when needed.
    expect(true).toBe(true);
  });
});
