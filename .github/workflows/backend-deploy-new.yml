name: Backend Deploy (Deprecated)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow notice
        run: |
          echo "This workflow is deprecated. Use backend-deploy.yml instead."
