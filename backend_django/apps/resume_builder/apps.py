from django.apps import AppConfig


class ResumeBuilderConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.resume_builder'
    label = 'resume_builder'
    verbose_name = 'Resume Builder'
